// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs';
import { Observable, of } from 'rxjs';
import { catchError, map, tap } from 'rxjs/operators';

import { MovingObjectDB } from './movingobject-db';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class MovingObjectService {

  httpOptions = {
    headers: new HttpHeaders({ 'Content-Type': 'application/json' })
  };

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  MovingObjectServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private movingobjectsUrl: string

  constructor(
    private http: HttpClient,
    private location: Location,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.movingobjectsUrl = origin + '/api/github.com/fullstack-lang/gongfly/go/v1/movingobjects';
  }

  /** GET movingobjects from the server */
  getMovingObjects(): Observable<MovingObjectDB[]> {
    return this.http.get<MovingObjectDB[]>(this.movingobjectsUrl)
      .pipe(
        tap(_ => this.log('fetched movingobjects')),
        catchError(this.handleError<MovingObjectDB[]>('getMovingObjects', []))
      );
  }

  /** GET movingobject by id. Will 404 if id not found */
  getMovingObject(id: number): Observable<MovingObjectDB> {
    const url = `${this.movingobjectsUrl}/${id}`;
    return this.http.get<MovingObjectDB>(url).pipe(
      tap(_ => this.log(`fetched movingobject id=${id}`)),
      catchError(this.handleError<MovingObjectDB>(`getMovingObject id=${id}`))
    );
  }

  //////// Save methods //////////

  /** POST: add a new movingobject to the server */
  postMovingObject(movingobjectdb: MovingObjectDB): Observable<MovingObjectDB> {

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)

    return this.http.post<MovingObjectDB>(this.movingobjectsUrl, movingobjectdb, this.httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        this.log(`posted movingobjectdb id=${movingobjectdb.ID}`)
      }),
      catchError(this.handleError<MovingObjectDB>('postMovingObject'))
    );
  }

  /** DELETE: delete the movingobjectdb from the server */
  deleteMovingObject(movingobjectdb: MovingObjectDB | number): Observable<MovingObjectDB> {
    const id = typeof movingobjectdb === 'number' ? movingobjectdb : movingobjectdb.ID;
    const url = `${this.movingobjectsUrl}/${id}`;

    return this.http.delete<MovingObjectDB>(url, this.httpOptions).pipe(
      tap(_ => this.log(`deleted movingobjectdb id=${id}`)),
      catchError(this.handleError<MovingObjectDB>('deleteMovingObject'))
    );
  }

  /** PUT: update the movingobjectdb on the server */
  updateMovingObject(movingobjectdb: MovingObjectDB): Observable<MovingObjectDB> {
    const id = typeof movingobjectdb === 'number' ? movingobjectdb : movingobjectdb.ID;
    const url = `${this.movingobjectsUrl}/${id}`;

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)

    return this.http.put<MovingObjectDB>(url, movingobjectdb, this.httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        this.log(`updated movingobjectdb id=${movingobjectdb.ID}`)
      }),
      catchError(this.handleError<MovingObjectDB>('updateMovingObject'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error(error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {

  }
}
